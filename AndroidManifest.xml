<?xml version="1.0" encoding="utf-8"?>
<!--
/*
*Most of this doesn't need to be editted, King, unless you want to add an addition permission (unlikely)
*However, if you change the name of the application you will want to change activity android:name=".KingKlickUpdater"
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.roman.updater">
    <uses-permission android:name="android.permission.INTERNET" />
    
    <application android:icon="@drawable/icon">
        <activity android:name=".KingKlickUpdater" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>